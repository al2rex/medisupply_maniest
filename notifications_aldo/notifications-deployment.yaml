apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications
  namespace: notifications
  labels:
    app: notifications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications
  template:
    metadata:
      labels:
        app: notifications
    spec:
      containers:
      - name: notifications
        image: public.ecr.aws/n7w5a8q2/medisupply-notificacions:latest
        imagePullPolicy: IfNotPresent   # 👈 usa tu imagen local de minikube
        ports:
        - containerPort: 9020
      
