# ========================================================
# 1️⃣ Gateway principal (Kubernetes Gateway API)
# ========================================================       
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: kiali-gateway
  namespace: istio-system
spec:
  gatewayClassName: istio
  listeners:
  - name: http
    port: 80
    protocol: HTTP